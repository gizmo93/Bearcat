service glftpd
{
    disable = no
    type = UNLISTED
    port = 21
    socket_type = stream
    protocol = tcp
    wait = no
    user = root
    server = /glftpd/bin/glftpd
    server_args = -l -o -i
    instances = UNLIMITED
    per_source = UNLIMITED
    cps = 1000 1
    flags = REUSE
}
