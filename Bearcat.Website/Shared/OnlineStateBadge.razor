@if (Value is null)
{
    return;
}

<MudChip T="Domain.ValueObjects.OnlineState" Size="@Size" Variant="Variant.Outlined" Color="color">@Value</MudChip>

@code {
    [Parameter]
    [EditorRequired]
    public Domain.ValueObjects.OnlineState? Value { get; set; }
    
    [Parameter]
    public Size Size { get; set; } = Size.Small;

    private Color color;

    protected override void OnInitialized()
    {
        color = Value switch
        {
            Domain.ValueObjects.OnlineState.Online => Color.Success,
            Domain.ValueObjects.OnlineState.PartiallyOnline => Color.Warning,
            Domain.ValueObjects.OnlineState.Offline => Color.Error,
            _ => Color.Default
        };
    }

}