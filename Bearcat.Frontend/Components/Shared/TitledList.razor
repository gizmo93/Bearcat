<FluentStack Orientation="Orientation.Vertical" VerticalGap="15">
    @foreach (var item in ChildContent)
    {
        @item
    }
</FluentStack>

@code {
    [Parameter]
    [EditorRequired]
    public IEnumerable<RenderFragment<TitledListItem>> ChildContent { get; set; } = null!;
}